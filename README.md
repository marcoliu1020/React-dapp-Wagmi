## deploy contract on hardhat node
open terminal
```shell=
cd Hardhat
npm install
npx hardhat node
npx hardhat run scripts/sample-script.js --network localhost
```

## run React
open terminal
```shell=
npm install
npm start
```